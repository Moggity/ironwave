{
  "name": "ironwave",
  "version": "1.0.0",
  "description": "Self-hosted powerbuilding web app on a wave periodization system, with server-side JSON persistence.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "tunnel": "npx -y cloudflared tunnel --url http://localhost:3000",
    "test": "node --test test/*.test.js"
  },
  "keywords": [
    "fitness",
    "powerbuilding",
    "strength-training",
    "workout-tracker",
    "self-hosted",
    "vanilla-js"
  ],
  "author": "Martin Arocha",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
